package com.studentos.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder unit test for the :app module.
 *
 * Real unit tests for business logic live in the domain layer of each
 * feature module (e.g., :feature:attendance domain/calculator/).
 * The :app module itself contains minimal logic (navigation wiring,
 * DI bootstrapping) so this placeholder confirms the test infrastructure
 * resolves correctly.
 *
 * Expanded in task 10.2 (end-to-end UI tests).
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
