package com.studentos.feature.assignments.domain.model

/**
 * AssignmentFilter — Enum representing filtered assignment views.
 */
enum class AssignmentFilter {
    ALL,
    PENDING,
    IN_PROGRESS,
    TODAY,
    THIS_WEEK,
    OVERDUE,
    COMPLETED
}
